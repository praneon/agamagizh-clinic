{
  "name": "agamagizh-chatwoot-bridge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Thin proxy: site forms -> Chatwoot API (contact, conversation, WhatsApp template message). No DB — Chatwoot is the system of record.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
